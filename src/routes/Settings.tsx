import { StubPage } from "./StubPage"

export function Settings() {
  return (
    <StubPage
      title="Settings"
      description="Tenant settings, regions, white-label config (stub)."
    />
  )
}
